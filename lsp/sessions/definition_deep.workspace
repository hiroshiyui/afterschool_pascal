# This session names files on disk, so %ROOT% is substituted into the input
# and written back out of the golden, and the Content-Lengths are blanked --
# a URI whose length is the checkout's cannot be counted once. See run.sh.
