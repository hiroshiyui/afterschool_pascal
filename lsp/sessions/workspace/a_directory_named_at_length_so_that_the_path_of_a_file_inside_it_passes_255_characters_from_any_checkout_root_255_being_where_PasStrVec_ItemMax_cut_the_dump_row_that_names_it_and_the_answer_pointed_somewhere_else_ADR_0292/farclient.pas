{ The client of the module beside it. Both are at a path past 255 characters,
  so the row of the dump that names the declaration is the row that used to be
  cut (ADR-0292). }
program farclient(output);

import Far;

begin
  writeln(FarAnswer)
end.
