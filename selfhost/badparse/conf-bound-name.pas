{ 6.6.3.7: an index-type-specification's bounds are *identifiers* being
  declared, not the constants an array-type's index is written with. }
program p;
procedure q(a: array [1..2] of integer);
begin end;
begin end.
