{ The last few rules the other files here do not reach. }
program more(output);
const pi = 3.5;
var i: integer;
procedure q(x: integer); begin write(x) end;
procedure control(x: integer);
begin
  for x := 1 to 2 do i := 1
end;
begin
  i := q(1);
  case i of pi: i := 1 end;
  control(1);
  write(i)
end.
