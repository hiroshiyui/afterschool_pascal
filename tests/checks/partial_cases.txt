# Case-statements that name a subset of their enumeration on purpose, and
# enumeration constants no case-statement names at all (ADR-0145).
#
# Two entry forms:
#
#   routine:enumeration:n names N of M   the n-th case-statement over that
#                                        enumeration in that routine
#   enumeration:CONSTANT unused          a constant dispatched some other way
#
# The counts are the point. A constant added to an enumeration turns every
# `names N of M` over it into `names N of M+1` and fails the gate, which is
# exactly the moment `tyString` and then `tyOptional` needed a reader and did
# not get one. Fix the case or move the number, and say which in the commit.
#
# The bar for an entry is the one `unreachable_diagnostics.txt` sets: not "I
# could not make it crash" but "no program can reach this arm", with the reason
# it cannot. Where the reason is a guard, name the guard.

# --- a narrowed parameter -------------------------------------------------
#
# Five callers pass five contexts -- ctxEnumConstants, ctxFieldList,
# ctxFormalDisc, ctxVarDecl, ctxParamList -- and `what` is read nowhere else,
# so the five spellings are the whole domain. A sixth caller passing a sixth
# context is what this entry is here to make fail.
ParseNameList:ctxKind:1 names 5 of 79

# --- an expression walker cannot meet a statement -------------------------
#
# nkGoto, nkLabeled and nkLabelDecl are statement nodes. The parser builds them
# only from ParseStatement, so a tree rooted at an expression contains none of
# them, and each of these four walks an expression.
#
# The fourth constant each of them leaves off is nkConfArray, and its argument
# is its own: ISO 7185 6.6.3.7's schema is a type-denoter the parser produces in
# one position only, a formal parameter's, and BuildFormals takes that case
# before ResolveType is reached (ADR-0153). Its own children -- the index type
# and a component that is not another schema -- are resolved by ConfArrayType
# with the kind tested first. So no path reaches these four with one, which is
# the same shape ADR-0040 gave a bare schema-name one clause earlier.
EvalConst:nodeKind:1 names 58 of 64
CheckExpr:nodeKind:1 names 58 of 64
EmitExpr:nodeKind:1 names 58 of 64
ResolveType:nodeKind:1 names 58 of 64

# EmitAddress is the same walk with one more exclusion: a substring designates
# characters rather than storage, so ADR-0052 emits an assignment to one
# through the runtime and never asks for its address.
EmitAddress:nodeKind:1 names 57 of 64

# And the statement walker's own exclusion. nkLabelDecl is the label-declaration
# part of a block, which CheckBlock reads directly; it never appears in a
# statement-sequence.
CheckStmt:nodeKind:1 names 63 of 64

# --- the three dump walkers each own a third of the node kinds ------------
#
# DumpExpr, DumpStmt and DumpTypeExpr are entered from the walker that already
# knows which of the three a node is -- an expression, a statement, a
# type-denoter -- so each names its own third. This is the one group where the
# subsets are large, and it is also the group ADR-0103 gave a corpus to:
# tests/dumps/ enters all thirty-one walker procedures.
DumpExpr:nodeKind:1 names 17 of 64
DumpStmt:nodeKind:1 names 16 of 64
DumpTypeExpr:nodeKind:1 names 15 of 64

# --- an operator the arm above already took -------------------------------
#
# The folder refuses a real constant expression before this case is reached and
# opRealDiv and opExp are refused there; opSymDiff is a set operator, and
# 6.8.8's constant-expression has no set-valued fold.
#
# It is `:2` and not `:1` because ADR-0226 put a case-statement in front of it:
# the string-relational fold dispatches on the same enumeration, names every
# constant, and so needs no entry of its own. The ordinal moving is this gate
# working -- an arm inserted ahead of a catalogued one would otherwise silently
# re-point the entry at a different case.
EvalConstBinary:binaryOp:2 names 19 of 20

# EmitBinary takes four operators before it evaluates anything. The four
# short-circuiting ones go to EmitShortCircuit -- 6.8.3.3 makes `and then` and
# `or else`, and in this compiler `and` and `or` too, control flow rather than
# an instruction -- and `in` goes to EmitIn, its two operands being of
# different kinds. opSymDiff reaches EmitSetBinary once the operands are
# evaluated. What is left for this case is the arithmetic and the comparisons.
EmitBinary:binaryOp:1 names 14 of 20

# The two below it are nested inside its `opEq, opNe, opLt, opLe, opGt, opGe`
# arm, one for the float predicates and one for the integer ones, so their
# selector is those six and `opIn` is the one comparison-shaped operator that
# is not among them -- 6.8.3.5 makes `in` a set membership, and EmitSetBinary
# has it.
EmitBinary:binaryOp:2 names 19 of 20
EmitBinary:binaryOp:3 names 19 of 20

# EmitStringCompare is entered only from EmitBinary's string arm, which tests
# IsStringOrChar on both operands; `in` takes a set on its right and never
# reaches it.
EmitStringCompare:binaryOp:1 names 19 of 20

# --- a builtin routed by shape before it is routed by name ----------------
#
# CheckCall splits the string builtins off first -- they take a string rather
# than a number and each has its own arity -- so this case sees only the ones
# whose argument is arithmetic. AP 6.7.6.10's argcount and argument (ADR-0173)
# are taken by arms of their own above it, as eof and eoln are, and so is
# AP 6.8.9's try (ADR-0178), AP 6.4.14.6's take (ADR-0182) and AP 6.4.12.5's
# release (ADR-0206).
CheckCall:builtinKind:1 names 27 of 46

# EmitCall's first three are catch-all lists that mean to name every builtin,
# and each omits the same four. biBinding, biDate and biTime are the builtins
# whose result is a *record* rather than a value in a register, so none of them
# reaches an instruction-shaped arm: biBinding is taken by an `else if` at the
# top of EmitCall, and biDate and biTime never reach EmitCall at all -- EmitExpr
# has an IsTimeBuiltin arm above the call it would come through.
#
# AP 6.8.9's biTry (ADR-0178) is the fourth, and it is biBinding's reason: an
# `else if` at the top of EmitCall takes it. It is left *out* of these lists
# deliberately rather than named for tidiness. Each of their bodies is a
# fallback -- `eq`, `undef`, a zero -- so a biTry that ever arrived would be
# emitted as one of those silently, where an unnamed constant stops the
# compiler where it happened (ADR-0018).
#
# AP 6.4.14.6's biTake (ADR-0182) is the fifth and is a stronger case of the
# same argument: it has no arm in EmitCall *at all*, not even at the top,
# because 6.4.14.6 admits it in one position and EmitAssign lowers it there.
# A biTake reaching EmitCall would therefore be a call Sema should have
# refused, and stopping is the answer that says so. Naming it in these three
# would turn that into an `undef` and a program that quietly moved nothing.
#
# AP 6.4.12.5's biRelease (ADR-0206) is the sixth and is biTry's case exactly:
# an `else if` above these lists takes it, because its result is an `i32` from
# one runtime call and none of the instruction-shaped arms below could spell
# that. It is left out for the same reason as the other four -- a fallback
# body would emit something rather than stop.
EmitCall:builtinKind:1 names 39 of 46
EmitCall:builtinKind:2 names 39 of 46
EmitCall:builtinKind:3 names 39 of 46

# The fourth is the real-valued intrinsics, guarded by the arm above it that
# established the argument is real. argcount and argument never reach it: the
# first is an arm near the top of EmitCall and the second goes to EmitString.
EmitCall:builtinKind:4 names 18 of 46

# --- a required procedure the arm above already took ----------------------
#
# EmitStdProc's outer case handles eighteen; pack and unpack are taken by an
# `if` before it, because 6.6.5.4 gives them a shape no other required
# procedure has -- two whole arrays and an index -- and the emitter needs the
# two operand addresses before the dispatch rather than after it. AP 6.7.5.9's
# exit is taken by an `if` before it too, and is still *named* by the arm that
# says so, which is what keeps this count one short of the whole rather than
# two (ADR-0177).
EmitStdProc:stdProcKind:1 names 20 of 24

# The two below it are nested inside that case's own arms -- one in the
# `spReset, spRewrite, spGet, spPut, spUpdate, spExtend` arm and one in the
# three-seek arm -- and each names the whole enumeration except the four that
# no arm of the outer case can have narrowed to.
EmitStdProc:stdProcKind:2 names 15 of 24
EmitStdProc:stdProcKind:3 names 15 of 24

# --- constants nothing dispatches on --------------------------------------
#
# `stdKind` stood here until ADR-0232 removed the conformance modes. It was
# the enumeration this half of the gate was written for -- ordered rather than
# dispatched, so no case-statement named any of its three constants and a pass
# over the *sites* could not have found it at all.

# pack and unpack are compared with `=` in the four places that need them --
# CheckStdProc twice, EmitStdProc twice -- because each place wants one of the
# pair and the difference between them is a direction, not a dispatch.
stdProcKind:spPack unused
stdProcKind:spUnpack unused

# ========================================================================
# Dispatch written as an if-chain (ADR-0221)
# ========================================================================
#
# `routine:enum:n chains N of M` -- the n-th if-chain in that routine whose
# conditions ask a value for its own tag, `x^.kind = c`. It is the same claim
# the `names` entries make and it protects against the opposite failure: a
# constant left off a case-statement stops the program (ADR-0018), and a
# constant left off a chain takes the trailing `else` in silence.
#
# ADR-0220 is the one that got out. EmitString's arm for a literal is keyed on
# a node's kind, a constant reaches the code generator as a designator, and the
# value read four bytes of read-only data as a length -- at -O0 only, so the
# corpus agreed with it.

# --- a designator, and the six productions one can be -----------------------
#
# ISO/IEC 10206:1991 6.5.1 makes a variable-access an entire-variable, a
# component-variable (indexed or field), an identified-variable (`p^`), a
# buffer-variable, or a substring-variable; 6.8.6 adds a function-access, which
# is a value and not a variable. So a routine that walks or tests a designator
# meets nkVar, nkIndex, nkField, nkDeref, nkSubstr and nkCall and no others,
# and which of the six it names is decided by what it is asking.
#
# The rest of nodeKind is expressions, statements, declarations and type
# denoters, none of which can stand in a designator spine. That is a property
# of the parser rather than a convention: nothing else builds a spine.
#
# A new *designator* node kind is therefore the change these are waiting for,
# and it would fail all of them at once, which is the point.
IsDesignator:nodeKind:1 chains 6 of 64
IsConstantAccess:nodeKind:1 chains 4 of 64
RootDesignator:nodeKind:1 chains 3 of 64
ContainerOf:nodeKind:1 chains 3 of 64
PackedComponent:nodeKind:1 chains 3 of 64
ConstAccessNode:nodeKind:1 chains 3 of 64
IsCallValue:nodeKind:1 chains 2 of 64
VariantSelector:nodeKind:1 chains 2 of 64
ThreatSym:nodeKind:1 chains 2 of 64
EvalConstAccess:nodeKind:1 chains 2 of 64
RootName:nodeKind:1 chains 2 of 64
CheckWith:nodeKind:1 chains 2 of 64
HeapHeader:nodeKind:1 chains 2 of 64
EmitSliceValue:nodeKind:1 chains 2 of 64
StringConstOf:nodeKind:1 chains 2 of 64

# The two that reach a designator by way of 6.8.7.4's set-value, whose tokens
# are a subscript's: only nkIndex and nkSubstr can be the spine the parser
# built, because a set-value is written with brackets and nothing else.
SetValueTypeOf:nodeKind:1 chains 2 of 64
CheckSetValue:nodeKind:1 chains 2 of 64

# 6.8.8's constant-accesses are three: an indexed-constant, a
# field-designated-constant and a substring-constant. nkStructValue is the
# fourth thing a constant-definition's value may be and is not an access.
CheckConstDecl:nodeKind:1 chains 3 of 64

# --- a type-denoter, and the three node kinds one can be --------------------
#
# A type-denoter is a TypeExpr and deliberately not an Expr, so the node kinds
# it can be are its own: nkNamed for a type-name, nkSchema for a schema
# production, nkRestricted for 6.4.2.5's restricted type. The structured
# denoters -- nkArray, nkRecord, nkSetOf, nkFile, nkPointer -- are named by the
# case-statements above and never reach these three, which ask about a *name*.
CheckSchemaBodyNames:nodeKind:1 chains 3 of 64
InitialStateOf:nodeKind:1 chains 2 of 64

# --- the two mixed dispatchers ----------------------------------------------
#
# EmitString is the chain ADR-0220 was found in, and the only one here that
# dispatches on a node's kind *and* on its type in one set of conditions. That
# is why it cannot be a case-statement: nkBinary decides nothing on its own,
# the arm being chosen by IsText or IsStringType after it. Four kinds name
# themselves; every other node reaching it is a value whose *type* selects the
# arm -- a char, a variable-string, a fixed-string -- and the trailing `else`
# is the fixed-string one, which is where a fifth kind would land unseen.
#
# The other two enumerations its conditions test are not tags: `e^.bnOp` and
# `e^.clBuiltin` are ordinary fields, so binaryOp and builtinKind are outside
# this gate by the rule that selects it.
EmitString:nodeKind:1 chains 4 of 64

# DumpStmt's is the husk rule (ADR-0044) and not a dispatch over statements:
# nkWrite and nkRead are the two statement kinds Sema may have replaced with a
# call, so this asks those two and hands everything else to the case-statement
# below it, which is where the other statement kinds are named.
DumpStmt:nodeKind:1 chains 2 of 64

# --- symKind: what a name may turn out to be at one position ----------------
#
# Each of these asks about a name in one syntactic position, and the positions
# admit two or three kinds of symbol apiece. skConst, skField, skDisc and the
# rest cannot arrive: a constant is not invocable, a field has no linkage name,
# a discriminant is not a parameter.
ResultTypeOf:symKind:1 chains 2 of 12
ResolvePointer:symKind:1 chains 2 of 12
ResolveType:symKind:1 chains 2 of 12
CheckArguments:symKind:1 chains 2 of 12
NameForLinkage:symKind:1 chains 2 of 12
PutParamTypes:symKind:1 chains 2 of 12
BindProgramParameters:symKind:1 chains 3 of 12

# --- typeKind: the ordinal types, and the writable ones ---------------------
#
# 6.4.2.1 makes the ordinal-types the enumerated, boolean, char, integer and
# subrange types, and OrdinalHi names exactly those five because each has a
# different last value: t^.hi, maxint, 255, 1, EnumCount - 1.
#
# OrdinalLo names two, and the asymmetry is the answer rather than an
# omission: char, boolean and enumerated all have a *first* value of zero, so
# one `else` is right for the three of them at once. A sixth ordinal type would
# have to be added to both -- to OrdinalHi because its last value is its own,
# and to OrdinalLo only if its first value is not 0.
OrdinalHi:typeKind:1 chains 5 of 21
OrdinalLo:typeKind:1 chains 2 of 21

# The three ordinal types whose name is *spelled* in a trap message. An integer
# and a subrange are reported by their bounds instead, a bound the block
# evaluated having no spelling in the source (ADR-0133).
WriteOrdinalName:typeKind:1 chains 3 of 21

# 6.10.3 lets a write-parameter be an integer, a real, a boolean, a char or a
# string; EmitWriteArgs names the five that are *not* a string, the string
# forms going through EmitString instead. int64 is ADR-0128's and joins the
# integer arm.
EmitWriteArgs:typeKind:1 chains 5 of 21

# An array and a record are the two types that can *contain* a dynamic part, so
# both of these recurse -- into the element type, and into the last field. The
# third constant is named to be **excluded**, which is why the count is three
# and not two: a subrange's size is its host's whatever its bounds are, so it
# is answered before the discriminant test rather than by it. ADR-0133 is what
# made a dynamically bounded subrange reachable at both sites.
#
# Every other type kind falls to a trailing `else` that is correct today for a
# reason worth stating: a set and a file each have a size the runtime is told
# once, and no other type carries a discriminant at all.
DynamicExtent:typeKind:1 chains 3 of 21
CheckSchemaDomain:typeKind:1 chains 3 of 21

# --- chains the source-parsing reader could not see (ADR-0230) --------------
#
# The five below are not new chains; they are chains the regular expression
# that used to answer this question never matched. Four of them it missed on
# its own terms -- it required the text `^.kind`, and these read `.kind` of a
# record, or sit far enough apart that its eight-line window closed first --
# and the fifth is in the walker that replaced it.

# ADR-0038's `and then` and `or else`: the lexer joins two tokens into one
# word-symbol, and this asks whether the last two are exactly one of the two
# pairs. It is not a dispatch over the token kinds and the other 76 are not
# left off -- there is nothing for them to do here, the pairs being the whole
# of what the clause spells with two words.
LexIdentOrKeyword:tokenKind:1 chains 4 of 80

# 6.4.4's pointer domain resolves to a type-name or to a schema-name, and the
# two schema arms differ only in whether the pointer is an owned one
# (ADR-0181). Every other symbol kind is a name that may not stand there at
# all, and the trailing `else` is the diagnostic for exactly that -- so what
# the chain omits is reported and not silently accepted, which is the
# distinction this catalogue exists to make.
ResolvePendingPointers:symKind:1 chains 2 of 12

# 6.7.3.3: an actual must be a designator where the formal is a variable
# parameter, and may not be parenthesised. Both arms ask about skVarParam and
# the pair is one requirement in two faults; skParam is the value-parameter
# arm beside them. No other symbol kind reaches a formal-parameter position.
CheckArguments:symKind:2 chains 2 of 12

# The two node kinds a *condition* can be built out of, for ADR-0230's own
# walker: `not` and a dyadic operator are the only nodes that contain another
# expression a tag test could hide in. Every other kind is a leaf as far as
# this walk is concerned, so there is nothing to recurse into and the trailing
# fall-through is the answer for all 61 of them.
CollectTagTests:nodeKind:1 chains 2 of 64

# AP 6.7.7.6: what an `external` heading refuses in a parameter position -- a
# procedural parameter, which would cross as a code pointer and its link where
# C takes one word, and a variable parameter of a type C has no answer for.
# The other ten symbol kinds cannot appear in a formal-parameter-list.
CheckForeignHeading:symKind:1 chains 2 of 12

# ADR-0239's outline walker. The two type-denoters that contain a
# defining-point: an enumerated type's constants and a record's fields are
# names, and every other denoter contains *types* -- an array's component, a
# pointer's domain, a set's base -- which have no name for an outline to hold.
# So the fall-through is the right answer for all 61 of the rest.
DumpSymType:nodeKind:1 chains 2 of 64

# The four node kinds a block's declaration parts hold, which is the whole
# domain this walk has: a constant-definition, a type-definition, a
# variable-declaration's group and a procedure-declaration. Every arm is
# named and there is no trailing `else` at all, so a fifth kind of declaration
# would be reported as nothing rather than as a procedure -- the better of the
# two wrong answers, and the one ADR-0221 says a chain must be written for.
DumpSymDecl:nodeKind:1 chains 4 of 64

# Which word names a type-definition's denoter. A record and an enumerated
# type are the two an outline distinguishes, because they are the two with
# children under them; every other denoter is reported as `type`, which is
# what the trailing arm says for all 61 of them.
DumpSymDecl:nodeKind:2 chains 2 of 64

# --- AP 6.7.3.10.4's determining positions --------------------------------
#
# `Mentions` and `Determine` dispatch over the three parameter-forms a type
# parameter can be read out of, and the bound is 6.7.3.1's rather than a
# convention: a parameter-form is a type-name, a schema-name or a
# type-inquiry, plus AP 6.7.3.9's slice. The three named here are the
# type-name (nkNamed), the production (nkSchema) and the slice (nkArray with
# no dimensions).
#
# The rest of nodeKind is expressions, statements and declarations, which
# never stand in a parameter's type at all, and the denoters that do not
# determine -- a type-inquiry names a *variable's* type and mentions no
# parameter, and every remaining denoter is refused in this position by the
# parser, which is why `^T` has no arm (ADR-0254).
#
# A constant added to nodeKind fails both, and the question it asks is the
# right one: can a formal parameter be declared with the new shape, and if so
# does a type parameter show through it? Answering "no" is moving the number.
Mentions:nodeKind:1 chains 3 of 64
Determine:nodeKind:1 chains 3 of 64

# --- a construct identified inside a type dispatch ------------------------
#
# EmitAssign dispatches on the *target's type* and not on the value's node
# kind: the arms are a string, a text, a handle, a fallible with arms apart,
# an owned pointer, and then the ordinary store. Two of them ask
# `s^.asValue^.kind = nkCall` as part of a compound condition, because
# AP 6.4.12.7 and AP 6.4.14.6 each admit exactly one construct in that
# position -- a `take` -- and the test is how the emitter recognises it.
#
# ADR-0230's shape rule sees that as an if-chain over nodeKind, and it is
# right to: the shape is `x^.kind = c`, which is the shape it was written to
# find. What makes this entry honest rather than a silencing is that the
# trailing `else` here is not a fall-through nobody thought about -- it is the
# ordinary assignment, which every other node kind is *meant* to take. A
# constant added to nodeKind belongs there and needs no arm here.
#
# The question a reader should ask when this number moves is the one the
# comment above sets: can the new node kind stand as the whole right side of
# an assignment to a handle or an owned pointer? Sema admits one construct in
# each and it is a required function, so the answer is no unless that changes.
EmitAssign:nodeKind:1 chains 2 of 64

# --- what may cross into a task -------------------------------------------
#
# CheckTaskFormals asks two questions about a formal's *kind* -- is it a
# variable parameter, is it a procedural one -- and both are refusals with a
# reason of their own (AP 6.9.3.12). Every other symKind either cannot be a
# formal parameter at all (skConst, skType, skVar, skProc, skFunc, skDisc,
# skField, skModule, skIface) or is skParam, the value parameter, which is
# what the *type* test after the chain is about.
#
# So the trailing else is not a fall-through nobody thought about: it is the
# ordinary value parameter, and the question asked of it is asked in the two
# arms below the chain rather than in it. A symKind added to the enumeration
# belongs there only if it is a new *kind of formal parameter*, which is the
# question this number moving should make a reader ask.
CheckTaskFormals:symKind:1 chains 2 of 12

# --- send is emitted by an if-chain, not by a case ------------------------
#
# EmitStdProc dispatches on stdProcKind twice with a case-statement and once
# with an if-chain, and `send` is in the chain: its arm needs the channel's
# *element type* to choose between an address and a temporary, which is a
# test on a type rather than on the tag. AP 6.9.3.13, ADR-0268.
stdProcKind:spSend unused

# --- and so is wait, in the same chain and for a related reason -----------
#
# AP 6.9.3.14 (ADR-0312). `wait` sits in the if-chain beside `send` because
# its arm has to lend the handle before it can call anything, which is two
# instructions before the dispatch and not one arm of it. Sema's own
# dispatch on it is an if-chain for the same reason: the checks are on the
# argument's *type*, and a task is one handle-type among three.
stdProcKind:spWait unused
Mentions:nodeKind:1 chains 3 of 64
Determine:nodeKind:1 chains 3 of 64

# --- a category that constrains nothing has nothing to explain ------------
#
# AP 6.7.3.10.5 (ADR-0266) gives a type parameter one of four categories, and
# tcNone is the fifth constant: a parameter written `T: type`, which every
# generic written before that clause is. SatisfiesCat answers *true* for it
# whatever the type, so nothing ever reports about such a parameter, and
# WriteCatAdmits -- which is only ever asked where SatisfiesCat has already
# said no -- cannot be reached with it. The guard is that answer and is one
# line above the only two call sites, both in InstantiateGeneric.
#
# WriteCatName, the sibling routine, names all five: --dump-ast writes
# `type-parameter type` for an unconstrained one, so its tcNone arm is
# reached on every dump of a generic.
WriteCatAdmits:typeCat:1 names 4 of 5

# --- the statements that leave -------------------------------------------
#
# `Transfers` asks whether control reaches what is written after a statement,
# and the answer is yes for every node kind but three: 6.9.2.4's goto,
# nkLabeled -- which is looked *through*, `1: exit` being an exit -- and
# nkProcCall, whose `pcStd` then decides between 6.7.5.7's halt, AP 6.7.5.9's
# exit, AP 6.7.5.10's break, AP 6.7.5.11's continue and the ordinary call
# that returns.
#
# The trailing else is therefore the answer and not a fall-through: an
# assignment, a compound, an if, a loop, a with, a case and a defer all
# continue to the next statement, and the ones that *may* not -- an if whose
# two arms both leave, a case every arm of which does -- are deliberately not
# claimed here, that being a lattice over the whole statement tree rather
# than a test on a tag (ADR-0277).
#
# A nodeKind added to the enumeration belongs in this chain only if it is a
# statement that unconditionally leaves the block or the loop it is in, which
# is the question the number moving should make a reader ask.
Transfers:nodeKind:1 chains 3 of 64

# --- the formatter's two questions about a token --------------------------
#
# ADR-0279's `--format` reads the token stream and decides what goes between
# two tokens. Two of its questions are if-chains over tokenKind and neither is
# a dispatch that could leave a constant out.
#
# `FmtWidthOf` asks how wide a token is written, which is a subtraction --
# 6.1.10 gives every token an extent and ADR-0258 records it -- for every
# constant but the two the scanner *joins*: `and then` and `or else` are one
# token each whose extent covers only the first word (ADR-0038), so they are
# the exception and the trailing else is the rule. A token kind added to the
# enumeration belongs here only if it is a second such join, which is the
# question this number moving should make a reader ask.
FmtWidthOf:tokenKind:1 chains 2 of 80
#
# `FmtHasBlock` scans a procedure- or function-heading to its terminating `;`,
# and the four constants are the bracket pair twice over: what it is counting
# is paren depth, so that a `;` inside a parameter list does not end the
# heading. Every other token kind is passed over, which is not a fall-through
# but the whole of the scan.
FmtHasBlock:tokenKind:1 chains 4 of 80

# --- an enumeration nothing dispatches on ---------------------------------
#
# `fmtOpener` says what an `end` or an `until` is closing, and every one of its
# seven constants is *compared* rather than dispatched on: the formatter asks
# `FmtTopKind = opRecord` to tell a variant-part from a case-statement and
# `= opRoutine` to know a body's end from a block's, and there is nothing a
# case-statement over it would be for -- the answers are not seven arms, they
# are two tests and a pop.
#
# A constant added here is therefore not a case-statement's problem. What it
# *is* is a new kind of thing an `end` can close, and the place that has to
# learn about it is the `end` arm of FmtToken, which pops trailing opVariant
# and opOtherwise frames before the one it came for.
fmtOpener:opBegin unused
fmtOpener:opRecord unused
fmtOpener:opCase unused
fmtOpener:opVariant unused
fmtOpener:opOtherwise unused
fmtOpener:opRepeat unused
fmtOpener:opRoutine unused
