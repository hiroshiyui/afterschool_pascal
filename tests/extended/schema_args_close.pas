{ §6.4.8's actual-discriminant-part is separated by ',' and closed by ')' --
  unlike the formal-discriminant-part it selects from, which uses ';'. }
program SchemaArgsClose(output);
type v(n: integer) = array [1..n] of real;
var a: v(3;
begin end.
