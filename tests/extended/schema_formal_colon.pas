{ A parse error, so it needs a file of its own: the parser stops at the first
  one. §6.4.7's discriminant-specification is `identifier-list ':' name`. }
program SchemaFormalColon(output);
type v(n integer) = array [1..n] of real;
begin end.
