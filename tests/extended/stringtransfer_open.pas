{ §6.7.5.5's parameter list is not optional either: `readstr` alone is not a
  statement, where `readln` alone is one. One file per message, because the
  parser stops at the first. }
program StringTransferOpen(output);
var i: integer;
begin
  readstr;
  writeln(i:1)
end.
